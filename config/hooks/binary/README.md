# Deprecated

live-build only executes hooks under `config/hooks/live/` or `config/hooks/normal/`
(see Debian Live Manual — Binary local hooks).

Kernel renaming for this project lives in:

`config/hooks/live/50-kernel-symlinks.hook.binary`

This directory is intentionally not used.
